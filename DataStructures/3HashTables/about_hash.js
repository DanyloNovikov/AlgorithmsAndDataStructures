// Hash has key and with this key we can find our params in garbage
// Hash has a hash function
// Hash function run md5 gererator he generate MD5 hash and then convert to the index space.
// key is an index of value in the memory.
// Why we need to use Hash ?
// Because we have index in memory and all search will be O(1)

// Hash table
// insert O(1)
// lookup O(1)
// delete O(1)
// search O(1)

// we simple use the key for quick search
// What is a Collisions ?
// For example if we have 2 hash with same key this key will stack in memory.
//
// HashTable vs Array
//
// Array:
// search O(n)
// lookup O(1)
// push   O(1)
// insert O(n)
// delete O(n)
//
// HashTable:
// search O(1)
// insert O(1)
// lookup O(1)
// delete O(1)
